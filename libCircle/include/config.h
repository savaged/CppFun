/* 
 * update these to suit your needs
 */

const double R = 5; // Radius
const double H = 3; // Centre x coordinate
const double K = 2; // Centre y coordinate

const int STEP = 6; // Divisor of the points in the circumference, for instance, 30 would divide the 360 degrees to 12 points like hours on a clock. (Default is 1 for 360 degrees).
